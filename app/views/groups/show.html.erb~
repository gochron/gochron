<div class="row navbar-clearance">
  <div class="panel panel-default webform">
    <div class="panel-heading">
      <h4><%= "Group Details" %></h4>
    </div> 
    <div class="panel-body">
      <div class="form-group">
        <h6><b><%= "Name:" %></b></h6>
        <%= @group.name %>
      </div>
      <div class="form-group">
        <h6><b><%= "Description:" %></b></h6>
        <%= @group.description %>
      </div>
      <%= link_to 'Back',groups_path, class: "btn btn-primary" %>
    </div> <!--panel-body-->
  </div> <!--panel-->
</div> <!--row-->
<div style="text-align:center;padding: 10px 0 10px 0;">
            <% if @group.subscriber_ids.include? current_user.id %>
              <%= link_to '', subscribe_group_path(@group, request_type: params[:request_type], perform: 'Remove'), method: :post,class: "btn btn-default btn-sm glyphicon glyphicon-ok added-to-cal", title: "Click to unsubscribe from this group."  %>
            <% else %>
              <%= link_to '', subscribe_group_path(group, request_type: params[:request_type], perform: 'Join'), method: :post,class: "btn btn-default btn-sm glyphicon glyphicon-plus", title: "Click to subscribe to this group."  %>
            <% end %>
            <% if g.user_id == current_user.id %> <!-- current user is the group admin -->
               <%= link_to '',edit_group_path(g),class: "btn btn-default btn-sm glyphicon glyphicon-pencil", title: "Edit" %>
               <%= link_to '',group_path(g),method: :delete, data: {confirm: 'Are you sure?'},class: "btn btn-default btn-sm glyphicon glyphicon-trash", title: "Delete" %>
               <%= link_to 'Add Events', new_event_path({group_id: g.id}), class: "btn btn-default btn-sm", title: "Add new events to this group." %>
            <% end %>
          </div>

